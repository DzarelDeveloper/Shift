{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Shift app",
  "permissions": [
    "core:default",
    "shell:default",
    "global-shortcut:default",
    "autostart:default",
    "fs:allow-read-file"
  ]
}
